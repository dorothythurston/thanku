<h2>all</h2>
<% @cards.each do |card| %>
  <div id= 'card'>
    to: <%= card.recipient_name %>
    from: <%= link_to card.sender_name, user_path(card.user) %>
    <%= card.body %>
    <%= image_tag card.image_url, width: 200 %>
  </div>
<% end %>
